[build]
builder = "dockerfile"
dockerfilePath = "apps/server/Dockerfile"

[deploy]
startCommand = "/usr/local/bin/ifc-lite-server"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
