export {} // module boundary (stripped by transpiler)

// Reset colors and visibility
console.log('[debug] Calling bim.viewer.resetColors()...')
bim.viewer.resetColors()
console.log('[debug] resetColors done')
console.log('[debug] Calling bim.viewer.resetVisibility()...')
bim.viewer.resetVisibility()
console.log('[debug] resetVisibility done')
console.log('View reset — all colors and visibility restored')
