---
"@ifc-lite/renderer": patch
---

Rework walk mode: arrow keys and WASD now move on a fixed horizontal plane with scene-proportional speed. Mouse drag in walk mode does full orbit (look around) instead of partial orbit + zoom. Remove orbit tool from toolbar — orbit is already the default mouse behavior for all tools.
